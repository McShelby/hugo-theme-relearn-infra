+++
title = 'Children Sort'
lastmod = 2024-01-01
+++

Two halves. [Sorted](sorted) calls the `children` shortcode once per documented
`sort` value over one set of pages. [Tags](tags) reaches the same shortcode the
way a taxonomy and a term page do, where the listing is built before the
shortcode ever sees it.
