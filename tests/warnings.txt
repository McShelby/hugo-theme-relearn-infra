# Accepted build warnings.
#
# Each line is a substring; a WARN or ERROR line containing it does not fail
# the run. Everything else does.
#
# This file is a list of known, outstanding theme work - not a place to
# silence noise. Delete a line as soon as the underlying issue is fixed, so
# that a regression fails the suite again.
