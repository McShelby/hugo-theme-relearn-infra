# The theme's own defaults, untouched: lazy and lightbox on, the rest off.
#
# An empty config is the point. Whatever a page shows here, it shows because the
# theme decided it, not because this fixture did.
