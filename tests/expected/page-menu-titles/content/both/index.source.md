+++
# Both keys set, which is what a site migrating off `menuTitle` looks like
# halfway through. `linkTitle` wins; the warning still fires, because the
# leftover key is what the author needs to be told about.
title = 'Both Section'
linkTitle = 'Both, from linkTitle'
menuTitle = 'Both, from menuTitle'
weight = 3
+++

Root of the `both` menu.
