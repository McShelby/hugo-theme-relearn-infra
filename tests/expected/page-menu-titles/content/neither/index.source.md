+++
# Neither key set, so the title falls through to the `neither-menuTitle`
# translation key. `title` is set and deliberately not what the menu should
# print: `linkTitle` falls back to `title` in Hugo, so a menu reading the
# method rather than the raw front matter would print this instead.
title = 'Neither Section'
weight = 4
+++

Root of the `neither` menu.
