+++
# `menuTitle` was removed in theme 6.0.0 in favor of Hugo's own `linkTitle`,
# but is still honored here as a deprecated fallback. Setting it deliberately
# provokes the theme's deprecation warning, which this site's warnings.txt
# accepts - see the note there.
title = 'Menu Title Section'
menuTitle = 'Menutitle, from menuTitle'
weight = 2
+++

Root of the `menutitle` menu.
